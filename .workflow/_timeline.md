# Timeline

Newest first. One line per change: date - capability-slug - one-line intent - record link. A recency lens over the same records the `_index.md` breadcrumb groups by capability; together they form the three-tier lookup (timeline -> index -> record). Never hand-pruned.

2026-06-30 - workflow-management-ui - folded the Handoff button into the Export dropdown (Export -> Workflow JSON / OpenSpec schema / Handoff package) so Workflow Management fits one row (Save . Clone . Export . Import); added a tooltip to the previously-untitled Clone button (Clone = save-a-copy: renames the workflow to "(copy)" so the next Save creates a new one, original untouched). Kept Clone's short label (renaming to "Save Copy" would have re-broken the one-row). Help modal + README refs updated to point at Export -> Handoff package. Also a focused TOOLTIP CONSISTENCY pass: added title tooltips to the toolbar mode buttons (Select/Connect/Delete with their 1/2/3 shortcuts - verified the keybindings exist), the view buttons (Auto Layout / Fit / zoom), and the five output tabs (Workflow/Sub-Agents/Agent Teams/Agent SDK/Claude.ai, condensed from the help modal). Deliberately skipped self-evident (Save/Import) and inline-documented controls (toggles, generate buttons). Markup only, 1172/1172 - .workflow/workflow-management-ui-and-tooltips.md
2026-06-30 - portable-agents-library - Agent Library (backlog #11): save a configured agent node as a named, versioned, shareable preset (localStorage awd_custom_agents) and drop it into any workflow pre-wired. Source-partitioned (user/org/builtin) with NAMESPACED ids so an org import can never clobber the user's own agents; version-aware merge (higher updates, equal/lower no-op); an org pack spoofing a user: id is re-namespaced to org. Mirrors custom-prompts (CRUD + export/import packs + form + REMOVABLE block). UI is its OWN toolbar "Agents" button + modal (not nested in the Prompt Library - decided after seeing the two toolbars collide); inert until used. Built end-to-end via the dogfood workflow (Planner->Implementer->Tester->Skeptic, memory+durable+grounding on) - findings in the record. +10 tests + UI screenshot. 1163/1163 - .workflow/portable-agents-library.md
2026-06-30 - openspec-schema-export - apply.requires now derives from the OUTPUT node's actual upstream work (via openSpecUpstreamWork, router-resolved, deduped) instead of hardcoding the last topo artifact. Makes the terminal gate faithful: a designer output that feeds a mid-chain step, or a foreign schema whose apply gates on a non-last step (e.g. apply requires a mid-chain review), is preserved + round-trips instead of being rewritten to the last step. Common linear case unchanged (output feeds last step -> same result). Output node config still drives apply.instruction (deliverable); this only fixes apply.requires. +2 tests (1153/1153) - .workflow/openspec-export-apply-requires-from-output.md
2026-06-29 - edge-routing - arc forward skip/bypass edges above intervening nodes (the forward mirror of the existing back-edge arc-below) so a same-row skip edge no longer cuts straight through the boxes between its endpoints. New connectionObstructed() (forward-only, excludes endpoints, vertical-band overlap); bezierPath/bezierMidpoint gain an `arc` param; render loop computes arc = !isBack && connectionObstructed(...). Geometry-only; adjacent + back edges unchanged. Surfaced by importing a real schema whose apply.requires pointed at a mid-chain artifact. +2 tests + headless screenshot. 1151/1151 - .workflow/edge-routing-arc-skip-edges.md
2026-06-29 - openspec-schema-export - FOREIGN-schema import (view a schema NOT created here): closes the deferred item. New REMOVABLE block - openSpecParseYaml (dependency-free YAML-subset parser for the OpenSpec schema shape), openSpecSchemaToWorkflow (artifacts->agent nodes, requires->edges, apply->output, layered layout by longest-path depth, cycle-guarded), openSpecImportForeign (pure orchestrator -> deserializeWorkflow-shaped object or null). Lossy by design (plain schema has no designer fields -> agentType general + default model/tools/positions, editable after). Only core touch: one guarded branch in importWorkflowFile. +6 tests; cross-checked against an openspec-CLI-generated schema.yaml headlessly (3 agents + Apply, deserialized). 1149/1149 - .workflow/openspec-foreign-schema-import.md
2026-06-28 - loop-prompt-library - add a "Loops" category to the Prompt Library with three fill-the-blanks loop builders (Converge to a Metric / Best-of-N explore-keep-best / Critique and Refine no-metric), chosen on the measurable-vs-judgment x refine-vs-explore axes so they are genuinely distinct. Each seeds its objective via the input popup and enforces the rest with a hard Step 0 "validate before looping, else STOP and ask" gate (the forcing function the single-field UI cannot give); variant-specific anti-failure rules - sacred measurement + cap + stall + honest exit (Converge), real-diversity + honest scoring (Best-of-N), adversarial critic + no-blocking-issues stop (Critique). Additive curated content, core untouched. +4 tests (1143/1143) - .workflow/loop-prompt-library-variants.md
2026-06-28 - openspec-schema-export - DRY: consolidate the read-side 3-tier lookup prose into one source `groundingLookupSteps()` (canonical steps tiers/scanIndex/noTransitive/supersede/greenfield). consumeRecordsHint composes it byte-identically (verbatim strings + same `> - ` framing -> prompt outputs unchanged); openSpecGroundingBlock renders the read-relevant subset as plain bullets (text shifted to the fuller canonical wording, more faithful to the 3-tier lookup). SDK folded in too: genAgentSDK grounding now renders the shared steps via a new wrapComment() helper (gains the supersede+siblings steps it was missing; backticks stripped). ONE source of truth feeds all three consumers (prompt blockquote / SDK # comments / OpenSpec plain bullets). +3 tests (1139/1139) - .workflow/openspec-export-cross-tool-grounding.md
2026-06-28 - openspec-schema-export - cross-tool grounding (read side, backlog #9): an OpenSpec run authored by our exporter now reads our `.workflow/` records. New gated pure helper openSpecGroundingBlock() (compact distill of consumeRecordsHint, gated on state.consumeRecords) prepended to the FIRST artifact's instruction in buildOpenSpecSchema; downstream steps inherit via requires; self-gating on greenfield; help-modal reworked into a read-side-flows-in / write-side-does-not split; +3 tests (1137/1137). Reverse direction (OpenSpec drops breadcrumb+timeline into our index) captured as backlog #10 - .workflow/openspec-export-cross-tool-grounding.md
2026-06-28 - openspec-schema-export - durable-record expectation toast: when the Keep-a-durable-record toggle is on at OpenSpec export, the success toast (and a new help-modal paragraph) note that the run leaves OpenSpec's per-step records, not the single .workflow flywheel doc - the toggle drives the prompt outputs, not the export (which calls no gen* protocol). Messaging-only via a pure openSpecExportToast(name,durable) helper in the removable block; durable-off stays the plain line (no nag); +2 tests. Read-side cross-tool grounding parked as backlog #9 - .workflow/openspec-export-durable-record-toast.md
2026-06-28 - custom-prompts-library - make the Prompt Library user-extensible: add/edit/delete your own prompts (localStorage awd_custom_prompts), shown under a "My Prompts" category with Edit/Delete + a CUSTOM badge, plus JSON export/import for backup and sharing (dedupe-by-title); integrates via a single effectivePromptLib() seam that returns the exact PROMPT_LIBRARY reference when unused (inert) and appends custom last so built-in indices never shift; user title/desc escaped, works with search + favorites, local-first (no network) - .workflow/custom-prompts-library.md
2026-06-28 - openspec-schema-export - export a designer workflow as a valid OpenSpec custom schema + lossless round-trip import (full workflow in the awd:meta comment); WYSIWYG-completeness pass: every node type now reflects into the schema body (tasks->artifacts, output->apply deliverables, input->description, parallel any/race->branch prose, agent config/loop as intent); export changed from .md bundle to a real .zip of the openspec/schemas tree (real-time stamped); templates are now role-aware AND node-specific record skeletons (skeptic mirrors the VERDICT contract, tasks get a prefilled acceptance checklist, each encodes its graph position + multi-repo checklist + gate readiness; all 18 agent types covered incl. a docs skeleton for the content roles) instead of empty stubs; Repo Context Paths (binding rules + product docs) forwarded into every instruction + apply; validated vs the real openspec CLI (linear/parallel/decision/task + role-mix) - .workflow/openspec-schema-export-and-roundtrip.md
2026-06-23 - durable-record-protocol - documentation-only design overview of the durable-record system (four parts + three-tier lookup + six design decisions) added to TECHNICAL.md for a new maintainer - .workflow/write-a-design-overview-of-the-durable-record-system.md
2026-06-23 - consume-prior-records - teach the read-side grounding guidance (consumeRecordsHint + the genAgentSDK inline comment) the full three-tier lookup so it scans _timeline.md (recency) and _index.md (relevance), not index-only - .workflow/make-ground-in-prior-records-use-the-full-three-tier-lookup.md
