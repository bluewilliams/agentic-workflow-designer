# Timeline

Newest first. One line per change: date - capability-slug - one-line intent - record link. A recency lens over the same records the `_index.md` breadcrumb groups by capability; together they form the three-tier lookup (timeline -> index -> record). Never hand-pruned.

2026-07-15 - self-improvement-loop (update) - tuning gets eyes: run-fence notes (newest 5) and grounded counts now survive ingestion and feed the tuning prompt (trust the note over the counter); the taught toolbox extends to models, toggles, topology, revision caps, clarify-first. 1562 -> 1563 - .workflow/self-improvement-loop.md
2026-07-15 - self-improvement-loop (update) - tuning round trip completed: Import menu (file / clipboard with fence extraction) so Claude's tuned JSON pastes straight back; advisor nudge at 3 runs; tuning prompt gains the designer vocabulary + falsifiable per-change predictions. 1559 -> 1562 - .workflow/self-improvement-loop.md
2026-07-24 - effort-control - a per-step Effort lever in Node Configuration under Model, defaulted by a sidebar Default Effort that unset steps inherit at read time: per-step prose line (peer of modelContextNote), one workflow beat naming the session-level `claude --effort` reality, real per-agent output_config in the SDK, picker filtered to what each step's model accepts and downgrades never silent. Persistence inherited free; no step carrying effort emits nothing, proven byte-identical four times. 1532 -> 1559 - .workflow/effort-control.md
2026-07-24 - model-roster - Opus 5 added and defaulted, four superseded families retired, and the shorthand rule made safe: only the newest model in a family keeps its short alias so a pinned step can no longer be silently upgraded to its successor (Opus 4.8 and Sonnet 4.6 now emit full API ids). Also fixes the defect the retirement exposed: a node on a retired model emitted that raw value as its Task param while the panel showed a different model. 1528 -> 1532 - .workflow/model-roster.md
2026-07-13 - self-improvement-loop (update) - Run report Explain row preempts the toggles-off surprise: states its deliberate independence from memory/durable/grounding (telemetry vs work-state) and its harmless-when-unwanted design - .workflow/self-improvement-loop.md
2026-07-11 - self-improvement-loop (update) - grounding made VISIBLE: the orchestrator narrates what the scan matched and contributed (kickoff + final report) and the awd:run fence gains an honest used-only `grounded` slug array (omitted when none, toggle-gated) - the flywheel now shows its owner it is turning - .workflow/self-improvement-loop.md
2026-07-10 - cadence-granularity-and-clarify-sensitivity (update) - clarify toggle wording speaks user-perspective ("the workflow opens with clarifying questions" - sidebar + Explain); emitted contracts keep agent-audience "before planning" - .workflow/cadence-granularity-and-clarify-sensitivity.md
2026-07-10 - node-config-ux (update) - selection scroll made smooth for humans via the __instantScroll rig hook (the __autoConfirm precedent) + prefers-reduced-motion guard; the original instant-scroll rationale (headless flake) is handled by the hook, not by denying the animation - .workflow/node-config-ux.md
2026-07-10 - prompt-library-overhaul (update) - category data model cleaned under the no-compat license: requirement badges moved from HTML-in-name strings to a `requires` field (names plain, test-pinned), 'memory-mode-portable' jargon made public-readable, code-search example standardized on Sourcegraph library-wide (badge + 24 in-prompt mentions + pin); all 75 titles reviewed and deliberately kept - .workflow/prompt-library-overhaul.md
2026-07-07 - verification-instructions (update) - the OpenSpec context line now carries the posture both ways (fail-the-run vs recorded-not-run-failing); foreign OpenSpec runners previously executed the checks but were never told blocked required ones should fail the run - .workflow/verification-instructions.md
2026-07-10 - agent-library-button - Add to Canvas dressed as a real row action (btn sm + tooltip) instead of the borrowed Prompt Library copy-chip style that read as disabled - .workflow/agent-library-button.md
2026-07-10 - prompt-library-overhaul - the prompt library gets its quality campaign: all 75 entries graded against the wave A-C bar (62 strong, 13 targeted fixes, 0 rewrites) - audit prompts now report what came up CLEAN as signal, web-dependent research names its no-web fallback, one example-key spirit leak fixed; 5 structural guards pin the bar. 1521 -> 1526 - .workflow/prompt-library-overhaul.md
2026-07-10 - unified-dropdowns - every dropdown wears the custom-select skin (director ruling: the Default Model look wins): enhanceSelect() wraps the three native selects with the native authoritative underneath (real change dispatch, refresh hook at every programmatic-write site), full keyboard a11y with canvas-shortcut exemption, one-popup-open across all species; zero existing-test churn. 1513 -> 1521 - .workflow/unified-dropdowns.md
2026-07-10 - native-dark-scheme (update) - interim select-option styling removed, superseded by the unified skin; color-scheme dark retained for scrollbars and remaining OS chrome - .workflow/native-dark-scheme.md
2026-07-10 - native-dark-scheme - color-scheme dark at :root: open select popups and scrollbars render in platform dark variants (the one surface CSS cannot style directly); custom dropdown rejected as over-engineering - .workflow/native-dark-scheme.md
2026-07-04 - explain-lever-audit - the name-your-lever principle enforced everywhere: all Explain OFF rows rewritten to name their lever in user language (the Datadog/code-search step hints carried the same internal-constant disease the principle was coined from), ON reasons use plain UI names, the Claude.ai Model row is an honest by-design skip, rules (h)/(i) share one consumer predicate, and TECHNICAL/README/help staleness swept. Sourced from the overnight read-only audit. 1511 -> 1513 - .workflow/explain-lever-audit.md
2026-07-04 - context-agent-types (update) - advisor rule (h) extended to the shared consumer predicate (legacy researcher-typed explorer carriers now get the missing-path nudge; third state closed) - .workflow/context-agent-types.md
2026-07-03 - context-agent-types (update) - App Under Test / UI Context become ALWAYS-PRESENT sidebar sections: the reveal machinery (type checks, legacy fallback reveal duty, transition auto-expand) deleted per the director's discovery ruling - summon-to-unlock was backwards, a collapsed section advertises where a hidden one cannot; consumer hints added, Explain OFF rows reworded to configuration guidance; plus advisor rule (i), the inverse of (h): a set App Source with no consuming step draws a stale-config nudge (mutually exclusive by construction). 1511 -> 1510 (net: 3 reveal tests deleted, 2 added, 2 migrated) - .workflow/context-agent-types.md
2026-07-03 - sidebar-collapse (update) - the auto-expand inventory returns to selectNode + the preset link: the same-day reveal-transition entry is superseded (App Under Test / UI Context went always-present; nothing conditional remains to auto-expand) - .workflow/sidebar-collapse.md
2026-07-03 - context-agent-types (update) - three same-day director rulings landed on the unit: the App Explorer TYPE default becomes a general application-cartographer template (selector prompt frozen byte-identical as uiAppExplorer, owned by the test_automation preset; reveal fallback matches both texts); selection-as-intent superseded by an explicit App Source Access selector (readonly default byte-identical, writable unconditional on tools - zero contracts consult tool selections); docs/help/README generalized. 1506 -> 1511 - .workflow/context-agent-types.md
2026-07-03 - context-agent-types - App Explorer + Design Analyzer promoted from smuggled preset templates to first-class agent types (16 total): read-only-explicit descriptions and tool floors, full registry wiring, reveal-by-type with legacy fallback (edited prompts no longer hide the App Under Test / UI Context sections), transition-only auto-expand past the first-run collapse seed, presets + auto-builder retyped, advisor rule (h) for a missing App Source with a calibration-pinned one-suggestion lifecycle, Explain levers named. 1492 -> 1505 - .workflow/context-agent-types.md
2026-07-03 - sidebar-collapse (update) - the conditional App Under Test / UI Context sections joined the programmatic auto-expand inventory (hidden-to-shown reveal transition only; a user re-collapse is never fought) - .workflow/sidebar-collapse.md
2026-07-03 - agent-type-tool-defaults (update) - registry gains the two read-only context-type floors; the old pre-web-tools debugger set is now an owned default under matches-ANY-default - .workflow/agent-type-tool-defaults.md
2026-07-03 - defect-posture (update) - reshaped per director rulings: knob decluttered to the coder family; the Reviewer gains its own flag-back posture (spot pre-existing defects, report to the orchestrator, never fix, never fail the gate) with topology-grounded dispatch (the review gate's drawn revise edge only, post-pass, outside gate cycles; record-only when undrawn) and posture-on-subtitle canvas visibility. 1481 -> 1491 - .workflow/defect-posture.md
2026-07-03 - defect-posture - the found-defect protocol's fix/record line becomes a per-node dial: Defect Posture select on building/testing nodes (Balanced default byte-identical to the shipped protocol; Record-only for surgical diffs; Aggressive for declared-and-tested opportunistic healing), reviewer scope judgment follows the declared posture, Explain names the lever. Born from the reviewer Explain-row confusion conversation. 1474 -> 1481 - .workflow/defect-posture.md
2026-07-03 - agent-craft-batch (update) - found-defect protocol amended in place by defect-posture (per-node posture selector; balanced default unchanged); History line appended, status stays current - .workflow/agent-craft-batch.md
2026-07-03 - agent-craft-batch (update) - item 9 docs sweep: README, TECHNICAL, and the help modal now cover the first-run experience (derived names, curated boot, quick start), repo-mandated verification files with owner targeting, Sonnet 5, and the wave's record sections (Verification ledger, Found bugs) - .workflow/agent-craft-batch.md
2026-07-03 - agent-craft-batch - the three dogfood runs distilled into agent craft: found-defect protocol (fix blocking or trivially-in-path with real coverage, record the rest - never work around, never silently fix), spot-mutation tester default (prove your tests can fail), verifier as explicit escalation tier (checklist-first evidence, harness bootstrapping, contract-form assertions, budget honesty), Found bugs + Verification ledger record sections (append-and-tick; blocked required checks visibly unticked forever), repo CLAUDE.md born, import/run-report polish. 1447 -> 1471 - .workflow/agent-craft-batch.md
2026-07-03 - verification-instructions (update) - repo-mandated verification files retargeted OWNER-ONLY by director ruling: the per-step contract goes to exactly one step (resolveVerifyOwnerId: last verifier, else last tester; reviewers audit, never execute; upstream testers receive nothing so expensive checks never run twice) and is explicitly additive to the owner's own verification method; Current behavior amended in place, status stays current - .workflow/verification-instructions.md
2026-07-03 - verification-instructions - team verification mandates become first-class: sticky verifyPaths config + posture checkbox drive a five-format orchestrator beat and a role-gated execute/na/blocked-with-reasons contract for testers/verifiers/reviewers (blocked required steps surface in verdict, record Verify, and awd:run - and fail the run when posture ON); silence-when-empty structural. THIRD DOGFOOD RUN (custom fork workflow, imported): FIRST REVISE CYCLE EVER - skeptic blocked the plan cycle 1 on a genuine five-format parity gap, re-spawned planner recovered from its memory file and passed cycle 2; first Verifier gate verified the delivery by independent execution; two harness dead-spawns recovered cleanly. 1398 -> 1447 with sibling sonnet-5-models - .workflow/verification-instructions.md
2026-07-03 - sonnet-5-models - Sonnet 5 + Sonnet 5 [1M] selectable everywhere: MODELS rows + taskModelMap base alias sonnet; 1M machinery generalized by suffix, zero special-casing; SDK full claude-sonnet-5 id. Sibling unit of verification-instructions in the same fork run; both its gates cycle 1 - .workflow/sonnet-5-models.md
2026-07-03 - sidebar-first-run - first-run experience: curated first-boot collapse seed (13 collapsed, requirements + presets open), empty-canvas two-step quick start, optional-name placeholder, and derived workflow slugs from requirements at all three slug sites (untitled collisions eliminated; explicit name wins byte-identically). SECOND DOGFOOD RUN - first under the v2.1 record protocol: all five one-shot sub-agents returned cleanly (A6 validated), grounding routed by role, both gates cycle 1; orchestrator finalize caught an lint-pattern test fixture. 1385 -> 1398 - .workflow/sidebar-first-run.md
2026-07-03 - sidebar-collapse (update) - Current behavior amended in place by sidebar-first-run (first-boot curated seeding at initSidebarCollapse; storage semantics untouched); History line appended, status stays current - the partial-amendment rule's first behavioral use - .workflow/sidebar-collapse.md
2026-07-03 - dogfood-run-fixes - everything the first live dogfood run taught, landed as one wave: valid Task model params (base alias + 1M prose note at five emission sites), conditional-honest Atlassian hints, and the memory protocol hardened where the run proved it load-bearing (first-turn breadcrumb clause in all four variants, orchestrator kickoff seeding, COMPLETE handoff mirrored into @slug.md as the recovery copy - all memory-gated with OFF assertions); one-shot spawn semantics stated for sub-agents; memory + durable record + grounding now DEFAULT ON. Durable record protocol v2.1: append-only History ledger, right-sized lineage (amend / partial / supersede), Grounds-on traversal line, section mutability rules, and grounding that routes record excerpts to every role, not just the planner. 1350 -> 1385 - .workflow/dogfood-run-fixes.md
2026-07-03 - sidebar-collapse (update) - retrofitted as the record-format v2.1 exemplar: History ledger (created + amendment lines) and a Links Grounds-on line (grounds on node-config-ux). The partial-amendment rule in action: the record stays current, gains a History line naming dogfood-run-fixes, no behavior change - .workflow/sidebar-collapse.md
2026-07-03 - sidebar-collapse - collapsible sidebar sections with persistence (chevron toggles, awd_sidebarCollapsed, Expand/Collapse All, auto-expand on every programmatic reveal protecting the node-config-ux scroll invariant). BUILT BY THE DESIGNER'S OWN GENERATED WORKFLOW - first live dogfood run: Feature preset, Sub-Agents format, Claude as orchestrator; both gates passed cycle 1; memory files carried the run through a response-channel failure; run report (awd:run) embedded in the record and dropped to ~/.awd. 1340 -> 1350 - .workflow/sidebar-collapse.md
2026-07-03 - revise-edge-continuity - the loop-back edge now tells you what a revise MEANS in the active output format: solid = the execution context persists (Workflow/Claude same session, Teams same persistent teammate), dashed = a fresh spawn (Sub-Agents briefed from blockers + memory file when on, SDK one-shot re-run). Hover carries the exact semantics (SVG title); the Explain revise-routing row speaks the same phrase from the same single-source helper (reviseContinuity()); help documents the legend. Loose coupling by construction: the renderer asks the pure helper behind a typeof guard and setExportFormat's existing render() is the entire tab linkage - removing the block restores prior rendering exactly (test-pinned). Owner idea born from the respawn-lossiness conversation; layered disclosure so the line style hooks, the hover teaches, Explain seals. +4 tests. 1336 -> 1340 - .workflow/revise-edge-continuity.md
2026-07-03 - prompt-overhaul-wave-c (update) - revise-loop continuity per format (owner: respawn-per-finding seems lossy): Teams revise now explicitly re-messages the SAME persistent teammate (context kept, no replacement spawns - the runtime always supported this; the prose said 're-assign'); Sub-Agents revise points the re-spawned agent at its own memory file when memory is on (Task agents are one-shot by runtime; surgical briefs + memory files are the continuity design); Workflow format loses nothing (single session). - .workflow/prompt-overhaul-wave-c.md
2026-07-03 - prompt-overhaul-wave-c (update) - final end-to-end coherence read (4 complete documents, ~295k chars) verdicted the campaign executable and left one seam: gate failure back-edges counted as DEPENDENCIES, so step-1 agents awaited reviewers that run after them (RCA Investigator awaited its own Verifier) - fixed with the same isReviseBackEdge predicate topologicalSort uses, plus review-loop dep resolution now surfaces the reviewed work alongside the critic's verdict; genWorkflow step hints moved before Instructions (were rendering inside the handoff list). Orchestration craft block landed same pass (brief-down-not-dump, evidence-honest gates, surgical revise briefs, absorb-nothing failure handling, fan-in reconciliation) + genWorkflow spawn/single-session contradiction fixed. +2 tests. 1334 -> 1336 - .workflow/prompt-overhaul-wave-c.md
2026-07-03 - prompt-overhaul-wave-c (update) - orchestration craft: the conductor gets the expertise treatment the agents got. New orchestratorCraft(format, hasParallels) emits five judgment bullets in Workflow/Sub-Agents/Teams (brief down not dump - signal not transcripts; grade gates on evidence honestly - never soften a failure because the run is long; surgical revise briefs - blockers verbatim + what was acceptable + cycle count; format-fitted step-failure handling - one sharpened re-run then record-and-decide, never silently absorb the step's job; fan-in reconciliation only when a fork exists). SDK/Claude.ai deliberately excluded. Fixed genWorkflow's Implementation Notes contradicting its own single-session execution model (told the orchestrator to spawn sub-agents for each step). Explain anatomy gained the row. +5 tests. 1329 -> 1334 - .workflow/prompt-overhaul-wave-c.md
2026-07-03 - prompt-overhaul-wave-b (update) - recency + reuse become builder/reviewer defaults (owner ruling: agents weight conventions by abundance, not recency - backwards mid-migration; and reuse-over-reinvention should be default). Implementer: follow where the codebase is GOING + reuse before you invent ('duplicating an existing capability is a defect, not a style choice'); frontend/backend compact same-discipline lines; reviewer: Conventions & reuse - judge the CURRENT direction, flag reinvention as material. Scope-creep + comment-discipline coverage audited and confirmed at three layers each, no change needed. - .workflow/prompt-overhaul-wave-b.md
2026-07-03 - tool-suggestion-semantics (update) - owner refinements: planner/architect regain WebSearch/WebFetch suggestions (suggestion semantics made lean pointless - generous costs nothing, and prior-art/capability lookup is core to these roles) with conditional craft lines in both rewritten templates; the suggestion clause gains a role-intent guard ('an investigative or review step should prefer reporting findings over changing things, unless its instructions say otherwise') as cheap insurance for write-without-Write drift; memory/durable protocols confirmed untouched by the semantics change. - .workflow/tool-suggestion-semantics.md
2026-07-03 - prompt-overhaul-wave-c - the campaign closer. Seven specialized templates rewritten expertise-first (threat-model security, hypothesis-first profiling, expand-contract migrations, least-privilege devops, churn-x-complexity analysis, traced-evidence architecture review, provenance-labeled business figures); the test-automation family now discovers its stack from the repo instead of assuming C#/SpecFlow/Appium; a 14th agent type Analyst lands fully wired (own template mapping, tool defaults, a dedicated analysis Skeptic lens for arithmetic/units/assumptions, both hint sets, bespoke OpenSpec sections, auto-builder retyped); two new presets ship the house idioms end to end (Analysis & Forecast with the analysis-lens gate and business finisher; Incident RCA where the VERIFIER proves the root cause by execution); advisor rule g catches review-role verdicts that route nowhere (type-gated after the calibration test caught it firing on post-gate testers). DRY of the test templates skipped honestly (no verbatim region survives post-wave-B). 1322 -> 1329 - .workflow/prompt-overhaul-wave-c.md
2026-07-03 - prompt-overhaul-wave-b - the philosophy sweep: core role templates rewritten to teach craft instead of tool mechanics (owner's insight; the critic prompts were the house style to converge on). planner/researcher/architect/investigator/reviewer/tester each gained their role's missing expertise (risk decomposition + resume units + assumptions/scope/stop; verified-vs-inferred + negative findings + confidence; options-and-losers + failure/scale + rollout; anti-anchoring + mechanism-not-vicinity; materiality bar + evidence rule + tests-as-hard-as-code; good-test bar) while the strong set (implementer/backend/frontend/fixer/writerTechnical/writerApi) had tool tutorials compressed into craft-first parentheticals and renumbered. Handoffs additive-only (Sequence/Assumptions/Out of Scope; Negative Findings; Options Considered; Hypotheses Eliminated). Researcher defaults +Bash (git-recency method). Honest size ledger: rewrites +4,279 chars, compressions -424, composed 4-agent workflow +3.6% - the ratio target (zero pure tool-tutorial steps) is met; expertise costs tokens. 3 test pins updated, none weakened. 1322/1322 - .workflow/prompt-overhaul-wave-b.md
2026-07-03 - prompt-overhaul-wave-a - the deep review's mechanical bugs, fixed as one unit: auto-builder security revise loop re-ran the reviewer instead of the builders (reviews-without-fixes until the cap; builders now captured before the security step consumes lastNodes); three presets (review/fullstack/ui_component) ended in dead-end reviewer verdicts and now carry proper gates with revise edges; the Skeptic's Convergence paragraph told it to PASS despite blockers after a cycle cap it could never know - both critics now keep verdicts honest always (the orchestrator owns proceed-after-cap; documented blockers = the record of what was accepted) and re-reviews are delta-scoped, with generators passing the cycle index at every revise emission site; phantom-role handoffs rephrased; analysis finisher -> writerBusiness; auto r1 mode-aware parity (record overclaim corrected); 11 planner/architect WebSearch grants removed per the lean ruling (testPlanner kept deliberately); PROMPTS.general goes codebase-conditional; writerUserguide can read what it searches (+WebFetch); Synthesizer/Advisor/Report Builder retyped writer. 1316 -> 1322 - .workflow/prompt-overhaul-wave-a.md
2026-07-03 - tool-suggestion-semantics - owner ruling superseding the closed-enumeration decision: tool selections are STRONG SUGGESTIONS, never restrictions. All prose formats now emit 'Suggested tools for this step: X' + one uniform suggestions-not-limits clause (any runtime tool fair game when the task calls for it); toolSubstitutionNote removed outright (no gap to reconcile when nothing is forbidden); the Agent SDK's hard tools=[...] param - the one true enforcement point - is GONE (suggestion rides as a comment + instruction lines; the memory-protocol Write-union machinery deleted with it; R3/C5 per-step-hint exclusion kept but re-rationalized as owner-priority minimalism). Ride-alongs from the deep prompt review: Verifier +Write in both pinned sets (its prompt writes a throwaway harness), Skeptic maxTurns 5 -> 8, debugger defaults +Write/Edit (repro test). tool-access-wording-and-dry.md is the archive's first resident (fence flipped, superseded_by linked, index entry moved). 1319 -> 1316 - .workflow/tool-suggestion-semantics.md
2026-07-03 - validation-modal (update) - danger red reserved for the irreversible: Replace (preset/Auto Workflow) and Clear (new workflow) confirms are undoable (preset load pushes a single undo point), so their buttons went from danger red to the standard blue primary (owner: the modal is the caution signal; color marks consequence). The three cannot-be-undone deletes keep red + focus-Cancel. - .workflow/validation-modal.md
2026-07-03 - self-improvement-loop (update) - Connect Folder guidance: Chrome's FS Access blocklist refuses the home directory root outright (owner hit "contains system files"), and ~/.awd does not exist until the first run writes it. Tooltip + help now teach: mkdir -p ~/.awd first, then pick the folder ITSELF via Cmd+Shift+G (never the home root). Copy-only. - .workflow/self-improvement-loop.md
2026-07-03 - mobile-viewport-scaling (update) - the owner's phone still clipped the bottom: 100vh includes the strip behind mobile Safari's bottom bar and the app suppresses page scroll, so the output panel was unreachable. App grid now height:100dvh with 100vh fallback (dvh tracks the visible viewport as browser bars show/hide; desktop identical). - .workflow/mobile-viewport-scaling.md
2026-07-03 - mobile-viewport-scaling - the viewport meta declared width=device-width (the responsive promise) over a fixed desktop grid, so phones rendered at device width and clipped the app. Swapped to width=1280: mobile browsers scale the full layout to fit (tiny but complete, pinch-zoomable), desktop ignores viewport meta entirely. Owner's ask was exactly scale-to-fit, not a responsive redesign - one line, zero desktop risk. - .workflow/mobile-viewport-scaling.md
2026-07-03 - validation-modal (update) - the native confirm() dialogs (owner: the last stock-browser UI) largely replaced by a themed confirm: appConfirm(message,{confirmLabel,danger}) -> Promise<boolean> on #confirmOverlay (help-modal classes, z-index 600 above all overlays, danger = red button + focus-Cancel safe default, Enter/Escape/backdrop, capture-phase trap so canvas shortcuts cannot fire underneath) + withConfirm(msg,opts,proceed) whose test hook (__autoConfirm) short-circuits SYNCHRONOUSLY - all 1314 legacy sync tests stayed valid with zero awaits. Six sites converted via _confirmed reentry (3 deletes, preset + auto-workflow replace, New workflow) with action-verb labels. DELIBERATE KEEP per owner skip-license: both secrets-copy confirms stay native (clipboard write must stay in the gesture task; the rig stubs the clipboard so a conversion is unprovable in CI) with source comments. +5 tests incl. the 6-converted/2-native invariant. 1314 -> 1319 - .workflow/validation-modal.md
2026-07-02 - explain-workflow (update) - the orchestrator capstone row gained its RUN PLAN: orchestratorRunPlan(format) derives the numbered kickoff / execution / close-out itinerary from the same sources the generators read (toggle-conditional kickoff, the emitted-set walk with format verbs, parallel launch-once with join phrases, gate routing with label fallbacks and cycle caps, delivery winner, record finalize, the awd:run fence), plus a summary snippet (steps, parallel groups, gates with worst-case revise spawns, critical path over the revise-edge-excluded DAG). Hand-built row keeps probe '' so the synthesized plan never leaks into the agreement matrix. +3 tests. 1311 -> 1314 - .workflow/explain-workflow.md
2026-07-02 - self-improvement-loop (update) - owner ruling: run telemetry is workflow-level, not repo-level. Every run now also appends its awd:run fence to ~/.awd/run-reports.md (the designer's canonical telemetry feed; the durable-record copy stays as archival context), self-gating if out-of-tree writes are not permitted; the fence gains an optional repo field for future segmentation; OpenSpec apply carries the same addition. One Connect of ~/.awd = every workflow against every repo, always on - the designer side needed almost nothing (flat-folder resolve, multi-fence extraction, and content-hash dedupe all existed). Connect UI/help/README lead with ~/.awd; per-repo connect demoted to alternative. +4 tests. 1306 -> 1310 - .workflow/self-improvement-loop.md
2026-07-02 - self-improvement-loop (update) - owner UX ruling: one home for the loop's read side. New "Run Reports" sidebar section directly below the Memory & Durable Record toggles (the record a run keeps is where its report comes back from) consolidating the Connect-repo state (moved out of the Workflow Management row, which was wrapping to two lines - now back to Save/Clone/Export/Import, test-pinned), a live status line (N reports ingested for the current workflow vs a no-reports nudge naming both feeds), and the Tuning prompt button (moved out of the Export dropdown; data-gating now in updateRunReportSection() riding the updatePrompt re-render seam + every ingestion incl. quiet rescans; ingestRunReport counts wf.reports). Section renders on unsupported browsers too - only the connect affordance hides. Help modal + README location wording updated. +2 tests. 1304 -> 1306 - .workflow/self-improvement-loop.md
2026-07-02 - explain-workflow (update) - first-use refinements from the owner: truncated anatomy rows unfold IN PLACE via details/summary ([view full] -> scrollable block inside the modal; inline chosen over any second surface), and input nodes are now explainable - explainInputNode() covers Requirements (present/empty + where they land per format), Source framing (URL-only resolve-once vs story/PRD framing), and Atlassian ticket fetch, statuses from the same helpers the generators call so the agreement invariant holds; the no-selection guard toast simplified. Also same commit-unit: the Explain step button moved from the node config panel (read as a stray tool chip under the chips) to sit beside the workflow-level Explain in the prompt actions, guard-and-toast over disabled-state plumbing. +3 tests. 1300 -> 1303 - .workflow/explain-workflow.md
2026-07-02 - explain-workflow - per-part anatomy of what the generators emit and WHY, making prompt composition permanently inspectable (the audit's invisible-composition bugs are the reason it exists). Workflow-level Explain beside Copy (orchestrator blocks: execution model, requirements/plan, delivery driver, every MCP/grounding/clarify hint, memory + durable protocols, decision section, run-report directive) and per-step Explain in the node config panel (template provenance via classifyAgentPrompt, the three tool lines, per-step memory, Datadog/code-search/record hints, Agent Context, dependencies incl. the deliberate fork-sibling omission, gates, delivery discipline, closing order) - each row emitted/skipped with the gating reason and a snippet. THE INVARIANT: statuses derive from calling the real helpers, emitted rows carry verbatim probes, and agreement tests assert probes against real generator output across a node/toggle matrix in all three core formats, so the explainer moves in lockstep with the generators or fails. SDK = one deliberate-exclusion note (owner unused). Removable block, reused help-modal classes, both entry points typeof-guarded. +8 tests. 1291 -> 1299 - .workflow/explain-workflow.md
2026-07-02 - self-improvement-loop - one unit of work closing the design -> run -> re-design loop. Durable artifacts v2: awd:record fence + present-tense Current behavior FIRST section (spec-vs-delta inside one file), granular live-ticked checklists with resume semantics, finalize self-lint (the agent is the linter), Archive as an _index SECTION (index stays one file at any scale), grounding reads Current behavior first / skips Archive / follows superseded_by in all consumers, 16 exemplar retrofits. Run-report contract: every run ends with ONE awd:run fence (steps status/turns/cap, gates cycles/verdict, omit-never-invent) via runReportDirective() in workflow/subagents/teams/claude (SDK comment line); finalize copies it into the record; OpenSpec apply emits the fence + a one-line reverse-grounding breadcrumb into the target repo's index/timeline (closes backlog #10). Designer read side: paste/drop ingestion (after awd:meta) OR one-time Connect-repo folder grant (FS Access API, IndexedDB handle, auto-rescan) with djb2 content-hash dedupe shared by both feeds; localStorage aggregates drive amber telemetry badges, a config Run history line, and the data-gated Tuning prompt (stats + awd JSON -> Claude returns an improved workflow for re-import). Advisor: adviseWorkflow() six non-blocking rules (reviewer-blocked BFS + one-click Attach Skeptic; custom-prompt exemption; telemetry rules) in the Workflow Review modal; badge stays issues-only; all 15 presets advise clean with zero tuning (calibration-pinned). Test harness upgraded to await async tests (deliberate-break canary). +33 tests. 1257 -> 1290 - .workflow/self-improvement-loop.md
2026-07-01 - undo-text-coverage - config text edits never pushed undo (Cmd+Z could silently wipe a hand-written prompt by jumping to the last structural snapshot) and every node mousedown burned an undo slot even for bare select-clicks. Text edits now undo at editing-session grain: the first input on a field since (re)build/blur snapshots the pre-edit state via a listener registered AHEAD of the state-writing handler (a keystroke burst = one entry; the expand modal is covered by construction since it dispatches the source field's own input events); drag snapshots are captured at mousedown but committed at drag-END only when the node actually moved (pushUndo gained an optional pre-captured-snap param). Behavioral tests written FIRST and seen failing for the right reasons: burst=one-entry, redo round-trip, blur splits sessions, structural-after-text undoes independently, 10-click select storm = zero slots, real drag = exactly one entry. 1251 -> 1257 - .workflow/undo-text-coverage.md
2026-07-01 - revise-loop-topo-generalization - the review-loop-topo-order fix's named follow-up: ANY decision's noLabel failure branch is now excluded from topologicalSort (the app's decision semantics are pass/revise everywhere - a revision path is never a dependency), via one shared isReviseBackEdge() that replaces both isReviewLoopBackEdge (its strict subset) and validateWorkflow's inline sibling-reachability copy, so the two can never disagree. All 15 presets verified byte-identical before/after by headless snapshot diff (their creation order already matched flow order); the change exclusively fixes hand-built revise loops created out of flow order (pinned by test: Tester-created-first now emits Planner -> Coder -> gate -> Tester) and the OpenSpec manual-loop export anchors requires to the real upstream. Zero existing-test changes. 1248 -> 1251 - .workflow/revise-loop-topo-generalization.md
2026-07-01 - validation-modal - the toolbar warning badge's detail view was the app's ONLY native alert() call (grep-verified); replaced with an in-app #validationOverlay reusing the help-modal classes wholesale (zero new CSS): one icon+message row per warning (warn amber, error red honored though only warn exists today), Esc/backdrop/close-button dismissal wired into the shared Esc handler ahead of help, zero-issues path keeps its toast. Presentation swap only - validateWorkflow and its warning data untouched. +2 tests (content verbatim via textContent, open/close, toast-only path). 1246 -> 1248 - .workflow/validation-modal.md
2026-07-01 - analysis-branch-prompts - the Auto Workflow analysis branch (Data Gatherer -> Analyst -> Report Writer) ran both agents on the generic codebase-research template whose "recommended implementation approach" contract does not fit measurement work. Two bespoke templates: analysisGatherer (concrete numbers with sources - Bash repo metrics, LSP usage surfaces, WebFetch pricing/benchmarks under conditional available-to-you phrasing; measured-vs-estimated tagged at collection) and analysisSynthesizer (visible arithmetic with units, justified assumptions, low/expected/high forecast ranges, independent sanity anchor, per-number confidence; Findings/Methodology/Assumptions/Recommendation handoff). The Data Gatherer's interim Agent Context license line is RETIRED - the guidance lives in the prompt proper, completing the tool-selection-consistency record's stated plan. 1246/1246 (test rewritten in place) - .workflow/analysis-branch-prompts.md
2026-07-01 - agent-type-tool-defaults (update) - defaults refinement + Agent Library form parity under the owner's make-it-the-best license and no-backward-compat ruling. coder/frontend/backend/debugger gain WebSearch+WebFetch and their four templates (implementer/frontend/backend/investigator) each gain one conditionally-phrased web step (consult official docs when implementing against external dependencies; search the exact error signature when investigating) - renumbered, nothing pinned. Reviewer checked and deliberately unchanged (template is read-and-analyze; runs nothing, so no Bash). Deliberate keeps recorded: planner/architect lean (repo-grounded; web research is the Researcher's job), adversary read-only vs verifier Bash+WebFetch (Skeptic reasons, Verifier executes). Outdated-pristine consequence pinned: old coder set still equals tester default so it swaps; old debugger set matches nothing so it is kept as customized (matching is against CURRENT defaults by design). applyAgentTypeChange (library agent form) now has FULL node-panel parity: pristine tool sets swap for ANY type change (was writer-only unconditional clobber), pristine role-template text re-bakes, customized tools/prompts never touched; two old-clobber tests rewritten to the new contract, two form tests added. +5 tests net. 1241 -> 1246 - .workflow/agent-type-tool-defaults.md
2026-07-01 - tool-selection-consistency (update) - owner flagged that the closed tool enumeration could forbid tools the designer does not model (MCP servers our own hints encourage, task tracking, notebooks) - the real runtime is bigger than the ten chips. New toolScopeNote(tools) right after the availability line at the same five agent-facing surfaces: names withheld CORE tools explicitly (deselection gets stronger - 'Core tools not listed above (Write, Edit...) are deliberately withheld') and declares everything outside the modeled set unrestricted ('MCP tools connected to your session, task tracking, and other runtime utilities remain available as normal'). Full selection = outside-clause only; empty list = nothing; SDK = outside-clause only (hard tools param already enforces core). Withheld list derives from ALL_TOOLS (no drift). Chip growth deferred deliberately (scope note covers the long tail by category). +1 test. 1240 -> 1241 - .workflow/tool-selection-consistency.md
2026-07-01 - tool-selection-consistency - two follow-ups completing the owner's tool model (selected = agent has it, deselected = not part of the prompt): preset testers gain Edit (9 tool arrays whose templates instruct modifying test files; Validator and Feature Writer deliberately left on their curated sets), and new toolSubstitutionNote(node) reconciles static role templates with the closed enumeration - when the effective prompt backtick-mentions a tool outside the node's selection, the five agent-facing surfaces (genWorkflow sequential+parallel, buildAgentPrompt, buildTeammateBlock, SDK instructions) emit one note naming the missing tools and telling the agent to skip or substitute; silent when the selection covers every mention or the tool list is empty (no enumeration = no contradiction). Chose a reconciliation note over selection-aware template rewriting (templates stay static/shared per the one-source architecture). +3 tests. 1237 -> 1240 - .workflow/tool-selection-consistency.md
2026-07-01 - agent-type-tool-defaults - owner ruling: an agent's role class implies a default tool floor (a Researcher generally SHOULD have WebSearch/WebFetch; a Coder needs write access; a Planner is read-only), with the user's deselect always winning and the per-step availability line staying the dynamic truth of the actual selection. New AGENT_TYPE_TOOL_DEFAULTS beside the other type tables (general derives from NODE_DEFAULTS so they cannot drift; adversary/verifier mirror REVIEW_LOOP_KINDS with equality test-pinned; writer resolves per style via the existing WRITER_TOOL_DEFAULTS through agentTypeToolDefaults()). The agentType-change handler pristine-swaps tools by the same matches-ANY-default rule matchesAnyRoleTemplate uses for prompts - tools and prompt follow the node to its new type together, and a user-customized tool set never swaps (this replaced the writer branch's old unconditional tool clobber; nothing pinned it). PROMPTS.researcher gains one conditional web step (10 of 13, "where external references would help and web tools are available to you...") so the template stays truthful whatever is selected. Presets and auto-built nodes are untouched (explicit curated arrays, no type-change event; curated sets deliberately match no default so later type changes preserve them). +8 tests incl. driving the real config-panel dropdown headlessly. 1229 -> 1237 - .workflow/agent-type-tool-defaults.md
2026-07-01 - auto-workflow-tailored-prompts (update) - restored WebFetch to the Auto Workflow Data Gatherer's default tools (owner ruling: never shrink a node's prior capability to satisfy prompt coherence - availability is a floor; a granted-but-unmentioned tool is milder than a wrongly limited agent, and Bash had been kept under the same conditions). Coherence solved from the other side: a new Agent Context line on the node licenses both out-of-template tools (Bash for repo metrics, WebFetch for external reference data) without touching the shared PROMPTS.researcher template. Bespoke analysis-branch prompt stays the named follow-up. +1 test. 1228 -> 1229 - .workflow/auto-workflow-tailored-prompts.md
2026-07-01 - node-config-ux - selecting a node gave no visible feedback (the config panel sits ~2621px below a 1000px viewport fold, sidebar never scrolled) - selectNode now scrolls the panel into view on a genuine selection CHANGE only (no scroll-jacking; instant, not smooth - smooth proved unreliable headless; measured 2621px-out-of-view -> 341px-in-view). Plus the type-switch bake trap: type change bakes the template only when the box is empty, so a SECOND change stranded the old role's full template under a status line claiming it derived from the new role - new matchesAnyRoleTemplate() (mapped templates + writer variants + adversary lenses) generalizes the Writing Style handler's pristine-swap rule: pristine template text re-bakes for the new type, user-customized text is never touched; classifyAgentPrompt gains a truthful 'foreign-template' state + Reset button for legacy saves. +3 tests + headless full-app smoke. 1225 -> 1228 - .workflow/node-config-ux.md
2026-07-01 - prompt-contract-polish - three generated-prompt quality items: closingOrderNote() gives agents ONE ordering sentence when 2+ end-of-response contracts stack (Handoff Summary -> DONE:/STATUS record lines -> memory breadcrumb very last; single-contract prompts stay unqualified; injected sub-agent/teammate/SDK); the unknown-agent-type fallback now composes from PROMPTS.general's body with the label/notes-seeded first line (near-duplicate literal deleted - no more drift); CODE_SEARCH_STEP_ROLES widened 8 -> 12 with reviewer/tester/adversary/verifier (the hint's cross-repo-seams rationale fits auditing roles; writer stays out; DATADOG_STEP_ROLES deliberately narrow - implementers inherit telemetry via the orchestrator's brief), C-series test literals moved in lockstep. +4 tests. 1221 -> 1225 - .workflow/prompt-contract-polish.md
2026-07-01 - auto-workflow-tailored-prompts - the Auto Workflow research branch claimed to mirror the Parallel Research preset but used generic PROMPTS.researcher everywhere (Options Researcher: 7 mandated LSP steps with no LSP tool, WebSearch granted but unmentioned; Synthesizer: the technical-writer documentation template). Now truly mirrors the preset: inferResearchMode/detectNamedOptions from the story, researchDocPrompt/researchPatternPrompt/researchSynthesizerPrompt with the preset's tool sets, fork carries researchMode so applyResearchMode works on auto-built workflows. Documentation tail: writer-lens Skeptic review loop ("Docs accurate?" gate, Passed/Needs revision) mirroring the documentation preset, linear writer-lens fallback for multi-branch tails; the old omit-decision-gate test rewritten to pin the sharper new contract. Data Gatherer drops unmentioned WebFetch (bespoke analysis prompt = named follow-up). Dead PROMPTS keys deleted: synthesizer, docResearcher, patternAnalyzer, codebaseExplorer. 1219 -> 1221 - .workflow/auto-workflow-tailored-prompts.md
2026-07-01 - sdk-toggle-parity - genAgentSDK silently dropped two toggles: clarifyFirst and mcpDatadog produced nothing in the SDK format. Both now render as orchestrator-level # banners from the same shared hint sources the prose formats use (clarifyFirstHint / datadogGroundingHint via the wrapComment pattern, markdown stripped; clarify's non-interactive branch already covers headless pipelines). KEY: per-step Datadog/code-search hints stay OUT of SDK agent instructions deliberately - tests R3/C5 pin the exclusion and the rationale holds (the hard tools=[...] param grants no MCP access, so the hint would be an un-executable instruction); an injection attempt was caught by those tests and reverted, with a source comment at the site. +4 tests. 1215 -> 1219 - .workflow/sdk-toggle-parity.md
2026-07-01 - delivery-ownership-subagents - the sub-agent format injected the full imperative delivery block (branch checkout, git push, gh pr create / Bitbucket API) into EVERY spawned agent's prompt via buildAgentPrompt, so a literal mid-workflow Planner or Reviewer was instructed to push and open the PR before downstream steps ran (and several agents could each open one). Delivery is now orchestrator-owned here like everywhere else: genSubAgents emits deliveryBlock('##') once in its orchestrator tail; per-agent prompts get the new deliveryAgentNote() - pr/commit = a short Delivery Discipline section (feature-branch only, never push or open the PR yourself, delivery runs once at the end), report/docs = empty (the final artifact is the orchestrator's synthesis), no output node = existing noCommitBlock verbatim (already the safe per-agent text). Other four formats verified already orchestrator-level, untouched. +2 tests (gh pr create exactly once; two per-agent discipline sections; no-output discipline retained). 1213 -> 1215 - .workflow/delivery-ownership-subagents.md
2026-07-01 - parallel-sibling-emission - all five generators emitted parallel groups with a blind `i += siblings.length` advance that assumed fork siblings sit contiguously in topo order; an interleaved dependency (fork->(A,B) + A->C->B) duplicated the whole A/B group and silently dropped C from every format (the SDK defined the agent but never ran it; the validator was silent). Every emission loop now walks with an emitted-id set: the group emits once at its first-encountered member, skips advance one at a time, and in-between nodes keep their own topo slot - byte-identical output for the normal contiguous case (whole suite unchanged). validateWorkflow now warns when fork siblings depend on each other ("cannot truly run simultaneously"); its reachability excludes any decision's noLabel failure branch so manual revise loops (test-automation preset) and attached review loops never count as dependencies. Headless before/after: Alpha/Beta 2 -> 1 each, Gamma 0 -> 1, SDK gamma.run 0 -> 1. Shares +10 tests with review-loop-topo-order. 1203 -> 1213 - .workflow/parallel-sibling-emission.md
2026-07-01 - review-loop-topo-order - attaching a Skeptic/Verifier mid-chain broke emitted step order in all four prose generators: topologicalSort treated the loop's failure back-edge as a dependency, the loop cluster went cyclic, Kahn's queue starved, and the creation-order fallback emitted the downstream Tester BEFORE the Skeptic gate (work ran on unreviewed output; the doc's own Depends-on contradicted its order). New isReviewLoopBackEdge() - identified exactly as detach/reroute do it (decision carrying reviewLoopDecisionFor, edge labelled with its noLabel; by label, not destination, because the branch can be rerouted upstream) - excluded from the ordering graph; the decision's forward edges remain real dependencies. The OpenSpec mid-chain export healed with zero exporter changes (was: gated artifact `requires: []` orphan root + apply tracking the skeptic; now: planner -> coder -> skeptic-review-coder -> tester, apply requires tester), pinned by regression test. Manual revise loops deliberately left on the old path (presets depend on the fallback's creation order; generalizing is a named follow-up). Headless before/after captured. 1203 -> 1213 (+10 shared with parallel-sibling-emission) - .workflow/review-loop-topo-order.md
2026-07-01 - generated-output-consistency - audit-driven consistency batch across the generators: genWorkflow parallel siblings now emit Agent Context (was the one spot still saying `Context:`), Max turns, and Success gate lines at parity with sequential steps (Depends-on deliberately skipped - the fork header already conveys it); empty-label decision fallbacks aligned to Yes/No at 10 sites so gate text and routing never disagree (decisions are created with Yes/No defaults; Pass/Fail only surfaced via imported blank-label JSON); 3 toolAccessText call sites guarded against empty tool lists per the helper's documented "caller guards emission" contract; em dashes purged from all emitted strings (raw and u2014-escaped forms: revise-cycle decision lines x2, TOON example, story placeholder, palette tooltip); validator empty-General warning copy updated post-PROMPTS.general ("generic scaffold - tailor it to your task"); Debugger AGENT_TYPES desc corrected to "Investigates bugs to root cause" (its investigator template hands off to a Fixer rather than fixing). +4 tests. 1199 -> 1203 - .workflow/generated-output-consistency.md
2026-07-01 - memory-write-authorization - the memory protocol orders every agent to append TOON entries to its memory files, but the closed per-agent tool enumeration contradicted that for Write-less agents (Skeptic/Verifier/preset Reviewer), and the SDK's hard tools=[...] param made compliance literally impossible (silent protocol dropout). Ruling: memory writes are protocol-level, not task-level - kept direct per-agent writes (orchestrator-transcribes stays a durable-record-layer pattern; mid-turn write-early-write-often is what survives compaction and cannot be mediated at handoff boundaries). New memoryWriteAuthNote() content helper injected at all four per-agent memory-write emissions; genAgentSDK unions "Write" into the emitted tools param when memory is on and the node lacks it, with a generated-code comment. All behind existing memoryEnabled gates. +4 tests. 1195 -> 1199 - .workflow/memory-write-authorization.md
2026-07-01 - general-agent-template - the "General" agent type mapped to the `researcher` prompt (AGENT_TYPE_PROMPT_MAP.general = 'researcher'), so resetting a General agent showed the codebase-research checklist and classifyAgentPrompt treated researcher text as the General template. Gave General its own neutral general-purpose template (`PROMPTS.general` - static "complete the task -> orient/plan/execute/verify/handoff" scaffold) and pointed the map at it (general -> 'general'). Kept the empty-General validation warning (validateWorkflow warns on empty General prompts but not typed agents - intent: General is a tailor-me scaffold; the warning checks the stored prompt so it's unaffected). Static (not the dynamic label fallback) so classify/Reset compare correctly; the fallback stays for truly-unknown types. No impact on imported agents (they carry a prompt). +2 tests (General != Researcher, end-to-end genWorkflow emits the general template). 1193 -> 1195 - .workflow/general-agent-template.md
2026-07-01 - agent-context-label - standardize the per-agent notes field's user-facing label on "Agent Context" (was "Additional context" in all 5 generated prompts + the OpenSpec export, while the UI already said "Agent Context"). Picked "Agent Context" because it matches the UI + pairs with "Workflow Context" (planInput) for a clean two-tier model. Swapped all 7 output spots (2 replace_all passes); did NOT rename the internal `config.notes` key (compat + churn). Fixed a stale TECHNICAL.md mapping line + reworded descriptive prose in README/TECHNICAL. Also confirmed (and left) presets correctly filling Agent Prompt not Agent Context. +1 test locking "Agent Context" across all 5 formats + OpenSpec (no test pinned the old string, so nothing broke). 1189 -> 1190. FOLLOW-ON: fixed the import<->export asymmetry it surfaced - export embeds notes into the artifact instruction as an "Agent Context:" block, but foreign import was pulling notes from the artifact `description` (metadata, not agent context) and leaving our block buried in the prompt. New openSpecExtractAgentContext() pulls our block back into notes + strips it from the prompt (no re-export dup); foreign `description` no longer mapped to notes (only backfills prompt when no instruction). Export unchanged/provenance-independent -> import+edit+re-export bundles Agent Context like native; awd:meta still lossless for normal round-trips, this fixes the awd:meta-stripped (openspec schema fork) case. +3 tests incl. full export->foreign-import->re-export. 1190 -> 1193 - .workflow/agent-context-label-standardization.md
2026-07-01 - expand-textarea-modal - summon a large modal editor for any text box (Requirements, Workflow Context, Agent Prompt, Agent Context, the Agent editor + Prompt Library add-prompt fields) via a discreet top-right corner button OR Cmd/Ctrl+E while focused. The modal is a REMOTE CONTROL of the real field: typing mirrors back to the source AND dispatches its own `input` event, so validate/updatePrompt/autosave/config-binding all fire as if typed inline (no save/cancel, no reconcile, regression-proof). DRY wiring: node-config fields get the button via the shared configTextarea(); static fields wrapped at init by an idempotent enhanceExpandableTextarea(). z-index 500 so it works modal-over-modal (from inside the Agent/Prompt editors); live word/char count + "changes save automatically" + caret preserved; Esc/Done/backdrop close. Additive + REMOVABLE (marked CSS/JS/markup). +7 tests (incl. a behavioral proof that a modal edit flips the Refine gate) + headless screenshots viewed. 1181 -> 1188. FOLLOW-ON same day: the agent-node context badge (top-right pencil, shown when an agent has Agent Context) was a passive indicator - now clicking it opens that context in the expand modal via editAgentContext() (guarded/removable; mousedown-stopPropagation so it doesn't drag; found via the pencil showing on imported schemas but not presets). +1 test + headless click proof. 1188 -> 1189 - .workflow/expand-textarea-to-modal.md
2026-06-30 - import-autofit - importing a workflow (awd:meta round-trip / foreign OpenSpec schema / plain JSON) now FITS the view instead of overflowing until a manual Fit click (presets already fit via loadPreset->autoLayout). Scoped to importWorkflowFile's 3 success branches, NOT deserializeWorkflow (shared by undo/autosave-restore, where an auto re-fit would fight the user). Tidy: `zoomFit` now SELF-DEFERS via setTimeout(0) so every caller just calls zoomFit() with no wrapper and no raw-setTimeout sprinkle; removed all 6 raw setTimeout(zoomFit,50|80) sites (import x3 + autoLayout + addFanIn + addFanOut) - they call zoomFit() directly, as the Fit button already did. Safe because the fit MATH is sync-correct (node w/h are static NODE_DEFAULTS; getBoundingClientRect forces layout - a sync fit took a 2140px/8-node import 1 -> 0.392), so a one-tick defer is strictly safe; verified no caller needs a sync fit. A named fitViewSoon() wrapper was considered then dropped for the simpler one-function surface (tradeoff: zoomFit is now fire-and-forget async). Headless-verified auto-fit post-change; 1181/1181 (no test change - async FileReader path, proven by harness) - .workflow/import-autofit-and-zoomfit-defer.md
2026-06-30 - tool-access-wording - align the per-node tool line on ONE phrasing ("You have access to these tools: X") across Workflow / Sub-Agents / Agent Teams / OpenSpec-export intent (SDK keeps the real tools=[...] param), replacing the two prior variants (bare "- Tools:" label + the sentence form). Semantics: availability, a CLOSED enumeration (no "minimally"/"at least" - that would make it a floor and re-authorize a deselected tool like WebSearch); prose = strong directional signal, only the SDK param is a hard block. Then DRY it: extracted the wording into a pure content helper toolAccessText(tools) and routed all 7 call sites through it, each keeping its own container (bullet/bold/indent/period/YAML) and emit guard - byte-identical. Rejected a monolithic renderAgent(node,{format}); model line left divergent on purpose. +4 tests (helper-direct + aligned-across-formats + old-labels-gone + golden exact-line pin). 1177 -> 1181 - .workflow/tool-access-wording-and-dry.md
2026-06-30 - refine-plan-clipboard - QoL: the Refine and Plan round-trip prompts now instruct Claude Code to put the result on the SYSTEM CLIPBOARD (in addition to writing the file) so paste-back into the Workflow Designer (Requirements / Workflow Context) is a single paste, not open-file-copy-paste. New pure helper clipboardCopyInstruction(filePath) - cross-platform PIPE form (cat FILE | pbcopy / xclip / xsel / clip.exe), clip.exe covers Windows PowerShell/cmd/Git Bash/WSL, graceful manual-copy fallback if no tool. Keeps the file (persistence + fallback). +1 helper test; 2 existing refine/plan tests updated to the new wording + clipboard assertions. Inline help notes mention it. 1177/1177 - .workflow/refine-plan-clipboard-roundtrip.md
2026-06-30 - input-gating - gate Generate Plan Prompt on the requirements input like Refine (disable when empty, with a why-tooltip) for a consistent affordance with Auto Workflow + Refine. Key decision: Plan gates like REFINE (needs non-empty), NOT like Auto Workflow (>=6 words prose) - because Plan/Refine work with a bare Jira URL and short specs (they fetch the ticket), while Auto Workflow's prose gate is specific to keyword-picking. planBtn id + validateStoryInput gating + disabled tooltips on Refine/Plan; click-time self-check retained; help modal updated. +4 tests (1176/1176) - .workflow/generate-plan-prompt-input-gating.md
2026-06-30 - workflow-management-ui - folded the Handoff button into the Export dropdown (Export -> Workflow JSON / OpenSpec schema / Handoff package) so Workflow Management fits one row (Save . Clone . Export . Import); added a tooltip to the previously-untitled Clone button (Clone = save-a-copy: renames the workflow to "(copy)" so the next Save creates a new one, original untouched). Kept Clone's short label (renaming to "Save Copy" would have re-broken the one-row). Help modal + README refs updated to point at Export -> Handoff package. Also a focused TOOLTIP CONSISTENCY pass: added title tooltips to the toolbar mode buttons (Select/Connect/Delete with their 1/2/3 shortcuts - verified the keybindings exist), the view buttons (Auto Layout / Fit / zoom), and the five output tabs (Workflow/Sub-Agents/Agent Teams/Agent SDK/Claude.ai, condensed from the help modal). Deliberately skipped self-evident (Save/Import) and inline-documented controls (toggles, generate buttons). Markup only, 1172/1172 - .workflow/workflow-management-ui-and-tooltips.md
2026-06-30 - portable-agents-library - Agent Library (backlog #11): save a configured agent node as a named, versioned, shareable preset (localStorage awd_custom_agents) and drop it into any workflow pre-wired. Source-partitioned (user/org/builtin) with NAMESPACED ids so an org import can never clobber the user's own agents; version-aware merge (higher updates, equal/lower no-op); an org pack spoofing a user: id is re-namespaced to org. Mirrors custom-prompts (CRUD + export/import packs + form + REMOVABLE block). UI is its OWN toolbar "Agents" button + modal (not nested in the Prompt Library - decided after seeing the two toolbars collide); inert until used. Built end-to-end via the dogfood workflow (Planner->Implementer->Tester->Skeptic, memory+durable+grounding on) - findings in the record. +10 tests + UI screenshot. 1163/1163 - .workflow/portable-agents-library.md
2026-06-30 - openspec-schema-export - apply.requires now derives from the OUTPUT node's actual upstream work (via openSpecUpstreamWork, router-resolved, deduped) instead of hardcoding the last topo artifact. Makes the terminal gate faithful: a designer output that feeds a mid-chain step, or a foreign schema whose apply gates on a non-last step (e.g. apply requires a mid-chain review), is preserved + round-trips instead of being rewritten to the last step. Common linear case unchanged (output feeds last step -> same result). Output node config still drives apply.instruction (deliverable); this only fixes apply.requires. +2 tests (1153/1153) - .workflow/openspec-export-apply-requires-from-output.md
2026-06-29 - edge-routing - arc forward skip/bypass edges above intervening nodes (the forward mirror of the existing back-edge arc-below) so a same-row skip edge no longer cuts straight through the boxes between its endpoints. New connectionObstructed() (forward-only, excludes endpoints, vertical-band overlap); bezierPath/bezierMidpoint gain an `arc` param; render loop computes arc = !isBack && connectionObstructed(...). Geometry-only; adjacent + back edges unchanged. Surfaced by importing a real schema whose apply.requires pointed at a mid-chain artifact. +2 tests + headless screenshot. 1151/1151 - .workflow/edge-routing-arc-skip-edges.md
2026-06-29 - openspec-schema-export - FOREIGN-schema import (view a schema NOT created here): closes the deferred item. New REMOVABLE block - openSpecParseYaml (dependency-free YAML-subset parser for the OpenSpec schema shape), openSpecSchemaToWorkflow (artifacts->agent nodes, requires->edges, apply->output, layered layout by longest-path depth, cycle-guarded), openSpecImportForeign (pure orchestrator -> deserializeWorkflow-shaped object or null). Lossy by design (plain schema has no designer fields -> agentType general + default model/tools/positions, editable after). Only core touch: one guarded branch in importWorkflowFile. +6 tests; cross-checked against an openspec-CLI-generated schema.yaml headlessly (3 agents + Apply, deserialized). 1149/1149 - .workflow/openspec-foreign-schema-import.md
2026-06-28 - loop-prompt-library - add a "Loops" category to the Prompt Library with three fill-the-blanks loop builders (Converge to a Metric / Best-of-N explore-keep-best / Critique and Refine no-metric), chosen on the measurable-vs-judgment x refine-vs-explore axes so they are genuinely distinct. Each seeds its objective via the input popup and enforces the rest with a hard Step 0 "validate before looping, else STOP and ask" gate (the forcing function the single-field UI cannot give); variant-specific anti-failure rules - sacred measurement + cap + stall + honest exit (Converge), real-diversity + honest scoring (Best-of-N), adversarial critic + no-blocking-issues stop (Critique). Additive curated content, core untouched. +4 tests (1143/1143) - .workflow/loop-prompt-library-variants.md
2026-06-28 - openspec-schema-export - DRY: consolidate the read-side 3-tier lookup prose into one source `groundingLookupSteps()` (canonical steps tiers/scanIndex/noTransitive/supersede/greenfield). consumeRecordsHint composes it byte-identically (verbatim strings + same `> - ` framing -> prompt outputs unchanged); openSpecGroundingBlock renders the read-relevant subset as plain bullets (text shifted to the fuller canonical wording, more faithful to the 3-tier lookup). SDK folded in too: genAgentSDK grounding now renders the shared steps via a new wrapComment() helper (gains the supersede+siblings steps it was missing; backticks stripped). ONE source of truth feeds all three consumers (prompt blockquote / SDK # comments / OpenSpec plain bullets). +3 tests (1139/1139) - .workflow/openspec-export-cross-tool-grounding.md
2026-06-28 - openspec-schema-export - cross-tool grounding (read side, backlog #9): an OpenSpec run authored by our exporter now reads our `.workflow/` records. New gated pure helper openSpecGroundingBlock() (compact distill of consumeRecordsHint, gated on state.consumeRecords) prepended to the FIRST artifact's instruction in buildOpenSpecSchema; downstream steps inherit via requires; self-gating on greenfield; help-modal reworked into a read-side-flows-in / write-side-does-not split; +3 tests (1137/1137). Reverse direction (OpenSpec drops breadcrumb+timeline into our index) captured as backlog #10 - .workflow/openspec-export-cross-tool-grounding.md
2026-06-28 - openspec-schema-export - durable-record expectation toast: when the Keep-a-durable-record toggle is on at OpenSpec export, the success toast (and a new help-modal paragraph) note that the run leaves OpenSpec's per-step records, not the single .workflow flywheel doc - the toggle drives the prompt outputs, not the export (which calls no gen* protocol). Messaging-only via a pure openSpecExportToast(name,durable) helper in the removable block; durable-off stays the plain line (no nag); +2 tests. Read-side cross-tool grounding parked as backlog #9 - .workflow/openspec-export-durable-record-toast.md
2026-06-28 - custom-prompts-library - make the Prompt Library user-extensible: add/edit/delete your own prompts (localStorage awd_custom_prompts), shown under a "My Prompts" category with Edit/Delete + a CUSTOM badge, plus JSON export/import for backup and sharing (dedupe-by-title); integrates via a single effectivePromptLib() seam that returns the exact PROMPT_LIBRARY reference when unused (inert) and appends custom last so built-in indices never shift; user title/desc escaped, works with search + favorites, local-first (no network) - .workflow/custom-prompts-library.md
2026-06-28 - openspec-schema-export - export a designer workflow as a valid OpenSpec custom schema + lossless round-trip import (full workflow in the awd:meta comment); WYSIWYG-completeness pass: every node type now reflects into the schema body (tasks->artifacts, output->apply deliverables, input->description, parallel any/race->branch prose, agent config/loop as intent); export changed from .md bundle to a real .zip of the openspec/schemas tree (real-time stamped); templates are now role-aware AND node-specific record skeletons (skeptic mirrors the VERDICT contract, tasks get a prefilled acceptance checklist, each encodes its graph position + multi-repo checklist + gate readiness; all 18 agent types covered incl. a docs skeleton for the content roles) instead of empty stubs; Repo Context Paths (binding rules + product docs) forwarded into every instruction + apply; validated vs the real openspec CLI (linear/parallel/decision/task + role-mix) - .workflow/openspec-schema-export-and-roundtrip.md
2026-06-23 - durable-record-protocol - documentation-only design overview of the durable-record system (four parts + three-tier lookup + six design decisions) added to TECHNICAL.md for a new maintainer - .workflow/write-a-design-overview-of-the-durable-record-system.md
2026-06-23 - consume-prior-records - teach the read-side grounding guidance (consumeRecordsHint + the genAgentSDK inline comment) the full three-tier lookup so it scans _timeline.md (recency) and _index.md (relevance), not index-only - .workflow/make-ground-in-prior-records-use-the-full-three-tier-lookup.md
