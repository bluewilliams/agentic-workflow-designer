# Live Run Monitor: the canvas watches the run

Context: no work item (director-commissioned flagship; vision approved, built in one delivery). Branch: main. Status: current. Repo: agentic-workflow-designer.

## Current behavior

The Monitor toolbar button (Chromium; hidden where the File System Access API is absent) opens a right-side drawer over the canvas that watches up to 4 directory handles (persisted in IndexedDB, key `monitorDirs`, shared `awdIdb` kv). A ~2.5s poll (paused while the tab hides) scans every watched tree bounded by depth/count caps, diffs `lastModified`+size snapshots, and re-reads only changed text files. The panel renders: the task checklist in three tiers (confirmed = `- [x]` in `tasks.md`; provisional = a normalized-substring match against agents' `t: {task} done` lines; open), with `(@slug)` owner tags; per-agent status chips from TOON entries (own file authoritative, a done handoff in `shared.md` wins, orchestrator log lines excluded); a change feed; and a click-to-view file list whose `.md` files render through `renderMarkdown` with an `awd:record` header chip (slug, status, date) - so a `.workflow` watch doubles as a durable-record browser. Canvas agent nodes carry live status dots (blue active, green done, red blocked/failed) through the guarded `monitorNodeStatus` hook, matched by label slug. `monitorRecompute` locates `tasks.md` at any depth and scopes the run surface to its directory: ONE watch covers a multi-repo run's live surface (the memory folder is shared across repos); extra watches add per-repo record browsing. Emissions carry the two-tier convention: every orchestrator memory variant seeds `tasks.md` (copied from the durable record's checklist when a record is kept - conditional at generation - else derived from the plan) and ticks each box individually as step reports confirm; every sub-agent appends `t:` lines to its OWN file at task completion (one writer per file, zero contention); the single-session variant ticks `tasks.md` itself. The record protocol adds owner tags, the kickoff tasks.md mirror, and a concision discipline. All parsing is pure string functions; the scan accepts any handle-iterator-protocol object (fake-handle tested). Everything is read-only and memory-gated with OFF assertions.

## Why and scope

Task lists tick in batches today because only the orchestrator may write the durable record (single-writer rule), and it learns of completed work when a sub-agent returns - often after ALL implementation tasks. The director wants live progress inside the designer. Design: the run's memory directory becomes a complete live telemetry surface the browser can watch (File System Access API, polling - no shipped file-watch events in browsers), and the emitted prompts gain a two-tier progress convention so granularity exists to display. The durable record stays the canonical task list; an ephemeral `tasks.md` in the memory directory is its live projection (hot-cache-over-durable-notes pattern). Scope: one removable monitor block + panel UI + canvas live badges + emission clauses + record-protocol quality pass. Chromium-only, hidden where unsupported, read-only by construction, paused when the tab hides.

## Requirements

1. Watching MUST require one directory pick (the memory dir or its parent), persist across visits via IndexedDB (repoConnect pattern), and re-arm with one click when permission lapses.
2. The panel MUST show: the task list with three visual states (open / provisional / confirmed), per-agent status, a chronological feed from `shared.md`, and a click-to-view file list. Canvas nodes MUST carry live status dots (running / done / blocked) matched by label slug.
3. Two-tier progress MUST be emission-backed: the orchestrator seeds and ticks `tasks.md` (confirmed tier, one writer); each sub-agent appends `t: {task} done` lines to its OWN memory file at the moment a task completes (provisional tier, single-writer per file, zero contention). The single-session variant ticks `tasks.md` directly (the orchestrator IS the executor).
4. Every emission addition MUST be memory-gated with OFF assertions; the record-protocol additions ride the existing durable gate (durable requires memory - the superset invariant holds).
5. Parsers MUST be pure functions over strings (TOON entries, checklists, provisional ticks) - fully unit-testable; the directory scan MUST accept any object speaking the handle async-iterator protocol (fake-handle testable, the scanRunReportsFromHandle precedent).
6. The record protocol quality pass: checklist boxes MAY carry an owner tag `(@{slug})` so tasks map to steps, kickoff mirrors the checklist into `tasks.md`, and the protocol gains an explicit concision discipline (the record competes with OpenSpec-style multi-file specs by being ONE dense navigable file - never padded).
7. The monitor MUST double as a durable-artifact browser: the file viewer renders markdown through the existing renderMarkdown pipeline, and a file carrying an `awd:record` fence shows a header chip (slug, status, date) - so picking `.workflow/` browses past changes as pleasantly as watching a live run.

## Approach and decisions

- Poll, do not push: FileSystemObserver is an origin trial only; snapshot-and-compare on `lastModified` + size every ~2.5s, re-reading only changed files, capped depth and file count. Rejected: watching arbitrary agent-edited repo files (git's job; tells WHAT changed, never WHERE THE RUN IS).
- `tasks.md` in the MEMORY dir rather than monitor-reads-the-record: one picked directory serves the whole monitor, and the record (repo-side) stays canonical. Rejected: dual connections (repo + memory) - friction kills the demo.
- Provisional ticks live in each agent's OWN file: preserves the single-writer-per-file invariant that shaped the memory protocol. Rejected: agents appending to tasks.md (write races) and agents writing the record (violates the one-writer-per-beat rule).
- The parked incremental-checkpoint patch is superseded by intent: these clauses are its purpose-built successor, written fresh against the current protocol. The parked artifact can be retired.

## Surface area (file -> role)

- index.html: LIVE RUN MONITOR removable block (state, idb reuse, scan, parsers, poll loop, panel render), toolbar button + panel markup + CSS, canvas status-dot hook (guarded), emission clauses in genMemoryProtocol / genAgentMemoryPreamble-Postamble / genSingleAgentMemoryProtocol / orchestrator kickoff sites, genDurableRecordProtocol checklist + kickoff lines.
- tests.html: parser units, fake-handle scan, merge logic, emission ON/OFF pins, panel DOM pins, exposure lines for monitor consts.

## Task checklist

- [x] Emission: tasks.md seeding + orchestrator tick discipline in both orchestrator memory variants (@orchestrator)
- [x] Emission: per-agent provisional `t:` clause in the per-agent and workflow-format memory beats (@orchestrator)
- [x] Emission: single-session variant ticks tasks.md directly (@orchestrator)
- [x] Record protocol: owner tags on checklist boxes + kickoff mirror line + concision discipline (@orchestrator)
- [x] Emission ON/OFF test pins across formats (@orchestrator)
- [x] Monitor core: state, supported gate, connect/persist/permission, resolve slug dir, multi-tree watches (@orchestrator)
- [x] Monitor scan: bounded recursive enumerate over the handle protocol + snapshot diff (@orchestrator)
- [x] Parsers: TOON entries, task checklist, provisional ticks, merge, agent statuses (@orchestrator)
- [x] Poll loop with visibility pause + changed-file re-read only (@orchestrator)
- [x] Panel UI: drawer, connect states, tasks three-tier, agents, feed, file viewer + record chip (@orchestrator)
- [x] Canvas live status dots matched by slug, guarded and removable (@orchestrator)
- [x] Parser + merge + fake-handle + DOM + emission tests green (@orchestrator)
- [x] Docs: README, TECHNICAL, help modal (Explain needs nothing: the memory row already unfolds the protocol text that names the monitor) (@orchestrator)
- [x] Review pass, fixes, three-surface record finalize (@orchestrator)

## Verify

- `./run-tests.sh` (1643 -> 1660: 6 emission pins + 11 monitor-core tests incl. fake-handle scan and full-panel render)
- Headless visual: panel rendered from fabricated state over the fullstack preset - three-tier ticks, owner tags, agent chips, feed, and live canvas dots all confirmed on screenshot

## Gotchas / non-obvious

- Browsers ship no file-watch events (FileSystemObserver = Chromium origin trial only); polling is the design, not a shortcut.
- Provisional-to-checklist matching is normalized substring matching - agents restate task names; exact-match would silently drop ticks.

## History

- 2026-08-25: created - design record authored as the spec before implementation (by direct session)
- 2026-08-25: built in one delivery per the director's ask - emissions (tasks.md + provisional t: lines across all four memory variants), record-protocol quality pass (owner tags, kickoff mirror, concision discipline), monitor block (multi-tree watches added mid-build on a director question about multi-repo runs), panel UI, canvas dots, docs. 1643 -> 1660, visual proof captured. Supersedes the parked incremental-checkpoint patch by intent (fresh clauses against the current protocol; the parked artifact can be retired) (by direct session)
- 2026-08-25: high-review fixes (8 finder angles; the parent reviewer stalled after finding, so adjudication ran in-session) - file-row handler injection killed (index-based selection; a filename with quote entities never enters a JS string), re-grant now restarts the poll timer, a lapsed watch no longer wipes the run surface (removals gated on actually-scanned prefixes), duplicate-label nodes get dots through buildAgentSlugMap, revise-dispatched agents show active again (ISO-timestamp ordering beats a stale shared done), short tasks gained a false-half-tick length guard, capitalized Tasks.md reads back, newest tasks.md wins across multiple watched runs, the slug-guess auto-descent dropped (could persist a stale handle forever), watch removal is by name (index race), per-watch scans + text reads parallelized, canvas repaints only when a status changed, panel keeps scroll position, feed capped by one const, help-modal Handoff heading un-orphaned, monitorSupported aliased. Deliberately kept: parseToonEntries body lines and scan-record mtime/size fields (both keep the pure layers testable with plain fabricated objects); emission-prose dedup deferred with a sync-comment instead (the three variants phrase deliberately differently around a shared contract). +6 regression pins, 1661 -> 1667 (by direct session)
- 2026-08-25: director hit the hidden-folder picker wall (~/.claude invisible in the macOS dialog) - the connect screen now carries a Copy Path button and the picker keyboard tips (Cmd+Shift+G paste-a-path, Cmd+Shift+. show-hidden, Chrome refuses the home folder itself - pick the run folder directly), the proven ~/.awd Connect Folder guidance pattern. Relocating memory to a visible folder was rejected: permanent Documents clutter, breaks the path convention in every emitted prompt and past record, helps no one on other platforms. 1667 -> 1668 (by direct session)
- 2026-08-26: pick-once auto-follow + Run selector (director UX ask) - run candidates are every dir holding tasks.md, shared.md, or progress.md (old-format runs count via shared.md; shared entries now seed a status when an agent has no own file, own-file authority and revise ordering intact); auto-follow tracks the newest activity, a Run selector appears when several runs exist (pin overrides auto, releases back), the panel names the active run, and Copy Path copies the workflow-memory PARENT. Also a public-content scrub found while checking the director's hardcoded-example question: an older committed record narrated a content-lint-sensitive ticket; fictionalized in place with a History line (auto-workflow-scoring-and-fuzz.md). 1668 -> 1671 (by direct session)
- 2026-08-26: feed readability (director's first-run screenshot showed run-path prefixes drowning every feed line) - feed lines compact to the file name inside the active run (last two segments elsewhere) with the full path in the hover title; Run selector options and the single-run line show the run dir NAME only. +1 pin, 1674 -> 1675 (by direct session)
- 2026-08-26: the deny-list whack-a-mole ended (director: "I don't want our solution full of code around ignoring certain file types") - the scan now honors each watched project's OWN .gitignore, the one general mechanism: every directory's ignore file governs its subtree, with a pragmatic pattern subset (name globs, *.ext, dir/, /rooted, path globs with * and **) plus real negation semantics (last matching rule wins - the first cut skipped negations, and the suite's own pin caught that this OVER-ignored). The built-in skip lists demote to fallback for folders without a .gitignore. Also mid-build: the invisible-character trap struck a fourth time (the glob compiler's sentinel transmitted as control bytes); repaired via count-checked script with printable escapes, byte scan clean. 1688 -> 1689 (by direct session)
- 2026-08-26: hash-stem asset filter (director spotted hex-named PNGs in Changes; verified on disk as bundler-emitted content-hash icon assets in an Electron build-output dir of ~500 entries - the source of that watch's capped flag) - the shape filter extends to hash-stem binary assets (12+ hex chars as the whole name, image/font extensions); a NAMED image stays visible, pinned both ways. Suite steady at 1688 (by direct session)
- 2026-08-26: final review closed (8 finder angles; adjudicated in-session) - the auto-follow run switch now clears the flash tracker (three finders converged: only the manual pin path cleared it, so a re-run with identical task texts false-flashed through the DEFAULT path); the stale-panel re-render signature covers the capped flag (same bug class one field over); _prevTiers joined the state declaration literal (the single inventory); the record's own History line stopped hinting at the private check's forbidden term (rephrased neutrally); the chip lookup collapsed to the active run's exact path (the null-base branch was unreachable); the wheel guard trimmed its unreachable existence checks (the typeof stays - it is the removability hook, documented in place). Efficiency and call-site finders came back clean. +1 auto-follow flash pin, 1687 -> 1688 (by direct session)
- 2026-08-26: dev-server noise (director's live watch showed hashed webpack/Ionic chunks flooding Changes - a dev server regenerates bundles in output dirs with generic names, so directory skips cannot catch them) - generated artifacts are now skipped by filename SHAPE at scan level (.chunk.js/.css, .map, .min.js/.css, .hot-update.*, content-hash .{8+hex}.js/.css) plus more output dirs (www, .angular, .ionic, platforms, plugins, .expo, .dart_tool); scan-level skipping also relieves the capped flag. Real source files (.ts, .cs) pinned to survive the filter. 1686 -> 1687 (by direct session)
- 2026-08-26: harvested review fixes (the stalled reviewer's finders all landed; adjudicated in-session) - a term the private hygiene check forbids had crept into three tracked record/timeline lines while DOCUMENTING the earlier scrub (renamed to content-lint phrasing; the check's naming rule now clean); ctrl+wheel/pinch over the open drawer no longer browser-zooms the page (the scroll fix had dropped preventDefault for modifiers); the chip click-through opens ACTIVE-run files only (the suffix fallback could show a stale run's evidence under a live status); the LIVE beacon re-renders on watch-status changes (a lapsed permission left it pulsing over a dead poll); _prevTiers clears on run switch and teardown (cross-run false flashes); the wheel guard costs one boolean when the drawer is closed; the parent-path derivation is one helper; a dead alias dropped. Deliberately kept: the hardcoded #monitorPanel wheel exemption (the wrap holds exactly one HTML overlay; a general overlay rule is speculation). 1686 steady, wheel + chip pins migrated to the sharper contracts (by direct session)
- 2026-08-26: the Files tab found its purpose (director: "I truly don't know what makes sense on this files screen") - the default is an ARTIFACTS view: durable records with superseded ones filtered by their own awd:record fence status, the _index/_timeline breadcrumbs, and OpenSpec docs, newest first; run machinery, READMEs, package.json, and code are excluded (the run has its own tabs; code lives in All). "All files" is the second mode: everything scanned, newest first - the what-did-agents-touch view. The mode control replaces the hide-run-files checkbox (subsumed: run files are never artifacts); chip click-throughs still pin their file visible in either mode; an empty Artifacts view explains where artifacts live. 1686 steady, tests migrated (by direct session)
- 2026-08-26: repo-watch fidelity (director watched a repo root and saw lodash flip-flop new/removed forever) - root cause: node_modules ate the 400-file budget and each cycle captured a DIFFERENT arbitrary subset. Three fixes: the scan skips dependency/build trees entirely (node_modules, .git, dist, build, vendor, venv, target, Pods, and friends - never agent work product); a watch that still hits the cap is flagged "capped" in its row, contributes no removals (a partial sample must not report phantom deletions), but still baselines silently and reports new/changed; the Files list now covers ALL scanned files (code included) ordered NEWEST FIRST with time labels - during a run it reads as "what the agents touched most recently" - with a graceful no-preview fallback for non-text files (the feed tracks them regardless). 1684 -> 1686 (by direct session)
- 2026-08-26: Files tab focus filter (director's dual-watch setup: memory for tasks + repo for files - the run machinery drowned the repo files) - run-dir files hide by default ("Hide run memory files" checkbox with a hidden count, shown only when runs exist); the tab count reads visible of total; a chip click-through pins its run file visible regardless; the toggle reveals everything. Rationale: the run machinery already has three tabs of its own. 1683 -> 1684 (by direct session)
- 2026-08-26: baseline scans are silent (director saw weeks-old runs flood the feed as "new" stamped with scan time) - a watch's first scan establishes the snapshot and loads contents without emitting feed events; only post-baseline changes are news. Per-tree: a later Add Folder baselines quietly while existing watches keep reporting. End-to-end pinned with permission-capable fake handles. 1682 -> 1683 (by direct session)
- 2026-08-26: the director caught that the header fix created a WORSE trap (two identical X glyphs 50px apart, one benign, one destructive) - destruction is never an X now: the remove-watch control is a text "Unwatch" button (title notes re-adding is one click), and exactly one X exists in the panel, meaning close; pinned as a count invariant. Suite steady at 1682 (by direct session)
- 2026-08-26: header polish + a UX trap defused (the director read the folder-row remove-X as a panel close - clicking it would have nuked the watch) - the panel header is now a flex row with a REAL close X in the top-right corner (title says watching continues in the background) and the LIVE beacon beside it, on both the connect and watching screens; the remove-X stays in its folder row with its distinct title. +1 pin incl. beacon-beside-close ordering, 1681 -> 1682 (by direct session)
- 2026-08-26: liveness layer + chip click-through (director gave design latitude: wow through LIVENESS, not decoration, in the app's quiet-pulse idiom) - a LIVE beacon while polling, a stacked progress bar (solid green confirmed + translucent blue provisional, width-animated), a one-shot flash on any task whose tier advances (the tick you SEE land), pulsing dots on active agents in both the chips and the canvas, color-coded feed kinds, and prefers-reduced-motion disables every animation. Extra-useful addition: agent chips click through to that agent's memory file in the Files viewer (status to evidence in one click; missing file toasts). All display-only. +3 pins incl. flash-once, 1678 -> 1681 (by direct session)
- 2026-08-26: tabbed panel (director found the stacked sections cluttered on a .workflow browse) - Tasks / Changes / Files tabs with live counts; smart default keyed on state already held (run candidates or tasks present = live watch, opens Tasks; files with no runs = records browse, opens Files), an explicit click wins for the session; header (watches, Run selector) stays above the tabs; per-tab empty states. +2 pins incl. content isolation, 1677 -> 1678 (by direct session)
- 2026-08-26: kickoff-before-clarify ordering (director's first live watch showed a dark panel: clarify-first was holding the run in questions BEFORE the memory kickoff ever ran, so no files existed to watch) - the clarify gate now instructs the orchestrator to run the memory kickoff FIRST, both so clarify answers can be logged to shared.md as the protocol already requires and so live tooling sees the run from its first moment; memory-gated with an OFF pin. 1676 -> 1677 (by direct session)
- 2026-08-26: scroll bug (director insisted after my wrong by-design answer - the drawer genuinely could not scroll) - the canvas-wrap wheel handler preventDefaulted EVERY wheel event including those bubbling from the drawer inside it, killing native scroll; events originating in #monitorPanel now bail before preventDefault, canvas zoom/pan capture intact elsewhere. Pinned both directions (drawer wheel not defaultPrevented, canvas wheel still captured). 1675 -> 1676 (by direct session)
- 2026-08-26: delta-review fixes (4/4 confirmed) - shared-only agents follow the full status progression instead of freezing on their oldest entry (own-file agents keep the done-only override), the clipboard promise gained its missing .catch (a denied write now toasts instead of silently lying), the connect screen shows the pick-once PARENT path so the displayed path and Copy Path agree (the old text invited copying a single run dir, defeating auto-follow), and Stop All plus watch removal clear the run pin (a stale pin survived and hijacked the next watch). +3 pins, 1671 -> 1674 (by direct session)
- 2026-08-26: capped-tree phantom-new fix (director report: untouched files fed as new every poll on a capped watch) - a capped scan sees a shifting partial sample, and only removals were guarded; never-sampled files dribbled in as phantom new events forever. General rule now symmetric: a capped tree feeds ONLY co-observed modifications (new AND removed absorbed silently; snapshot and texts still union-grow), no per-project knowledge involved. Tooltip states the contract. Pinned end to end with an over-budget fake tree (phantom new silenced, real modification feeds). 1689 -> 1690 (by direct session)
- 2026-08-26: scan budget resized (director report: even a src folder capped) - the 400-file / depth-3 bounds predated the gitignore-honoring scan and were sized against junk walks; with junk now filtered at the source the budget serves only to bound per-poll stat cost. MONITOR_MAX_FILES 400 -> 1500, MONITOR_MAX_DEPTH 3 -> 8 (real src nesting exceeds depth 3, silently truncating trees). The capped contract stays as the overflow backstop. Capped test scales via the exposed const; help modal, README, and TECHNICAL gained the watch-narrow tip plus the gitignore/capped contract (docs stated neither). Suite steady at 1690 (by direct session)
- 2026-08-26: scan/display decoupled (director diagnosis session: capped src with All showing 163) - the All count read Object.keys(texts) (text files only), lying against a snapshot-backed list and making a truthful capped flag look contradictory; count is now the snapshot-union-texts set the list actually renders. Watch rows show their per-tree scanned file count (self-diagnosing: the number tells you which tree is big and whether the cap bit). Two independent budgets per the director's insight (show far fewer, poll far more): MONITOR_MAX_FILES 1500 -> 4000 with big scans (over 1500 files) polling every second tick to keep per-second stat cost flat, and MONITOR_FILES_SHOWN = 100 caps only DOM rows (newest first, honest footer) - never what gets polled. 1690 -> 1691 (by direct session)
- 2026-08-26: silent-start gap closed (director spotted a running coder with no chip) - chips render disk truth, and the protocol's first mandated write came only after the first significant action, so an agent deep in reading had no file and no chip. Fixed from both sides: (1) emissions - agents open their own file with an active entry BEFORE the first action (genMemoryProtocol section 2 and the sub-agent preamble, now step 3 with t: ticks moving to step 4; the SYNC CONTRACT note updated), because a silent start is indistinguishable from a failed spawn; (2) monitor - tasks.md owner tags seed the run's roster, so a dispatched-but-quiet agent shows a gray pending chip immediately (a written file always wins; pending chip click toasts gracefully). Current runs get pending chips on the next poll; the start-entry needs regenerated prompts. 1691 -> 1693 (by direct session)
- 2026-08-26: roster-seeding orchestrator leak (director screenshot: an @orchestrator pending chip) - the pending-chip seeding read every tasks.md owner tag, but monitorAgentStatuses deliberately excludes the orchestrator (not a step agent, no own file, status never tracked), so its chip would sit pending forever. The seeding now applies the same exclusion; test extended with an orchestrator-owned box. Suite steady at 1693 (by direct session)
- 2026-08-26: extended review round 2 (range 37f7725..HEAD + working tree; 3 confirmed, 1 refuted) - (1) file selection moved from render-time indexes to a data-path attribute (the monitorRemoveWatch pattern, escHtml-escaped): kills the click-vs-poll re-sort race that could open the wrong file, injection contract re-pinned on the attribute form; (2) the display cap appends the SELECTED file past the 100-row slice so a poll never closes an open viewer, footer counts via the const; (3) monitorScanHandle bounds its entry buffer (a pathological flat directory no longer buffers every handle each poll; a .gitignore beyond the buffer is still captured by streamed name-matching and still governs) plus a budget-spent early return that skips enumeration entirely. Refuted: shared.md narration chips (the Step-N form never routes to shared.md on-protocol; old code chipped the same off-protocol case at done). A mid-round harness stall and one 3-minute bisect hang were the session crash brewing, not the code: scratch-dir bisect re-runs proved every combination green. 1693 -> 1694 (by direct session)
